#!/usr/bin/env python
# Format the requested document

from os import system,environ
from os.path import join
from sys import argv
from datetime           import datetime
from doc import doc_path, log_page


path   = ['','']
if len(argv)>1: 
    path = argv[1].split('/')

doc = doc_path(path)
log_page(doc)
print doc


