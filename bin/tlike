#!/bin/bash
# Accept the currect results as the correct answer

# Choose the correct directory (default to test)
f=${1/\.tst/}   
[ ! -f $f.out ] && f=$pt/$f

echo Accept results from $f.tst
cp $f.out $f.correct
rm $f.diff
touch $f.diff
