#!/bin/bash
# Enable a single application

user=$1
app=$2

# Remove the app
t=/tmp/hammer-disable$$
grep -v $2 $pd/$user/.apps > $t
mv $t $pd/$user/.apps

# Show the resulting apps
cat $pd/$user/.apps

