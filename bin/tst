#!/bin/bash
# Run all system tests

# Go to directory directory
d=$1
[ "$d" == "" ] && cd $p && d=test

# Must have some test files
ls $d/*.tst > /tmp/tst$$ 2> /dev/null

# Run each test
{
    for f in $d/*.tst 
    do
        trun $f
    done
    tfail $d
} | tee tst.out
