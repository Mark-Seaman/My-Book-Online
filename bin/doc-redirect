#!/usr/bin/env python
# Format the requested document

from sys         import argv
from util.domain import  doc_redirect


if len(argv)>1: 
    d = doc_redirect(argv[1])
    if d:
        print d
else:
    print 'usage: ', argv[0], 'url'
