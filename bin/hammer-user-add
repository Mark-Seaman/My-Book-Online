#!/bin/bash
# Add a new user

user=$1

cd $pd
[ -d "$user" ] &&
echo 'User already exists' &&
#exit 1

echo "Create user $user"
mkdir $user
touch $user/.apps
echo "hammer-index $user"      > $user/Index
echo "hammer-app-config $user" > $user/Apps
chmod +x $user/Index $user/Apps
