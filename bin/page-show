#!/usr/bin/env python
# Format the requested document

from sys       import argv
from util.page import  show_page


if len(argv)>1: 
    print show_page(argv[1])
else:
    print 'usage: ', argv[0], 'url'
